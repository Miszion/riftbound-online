{
  "name": "riftbound-online",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "build:static": "next build && next export",
    "deploy:dev": "npm run build:static && aws s3 sync ./out $DEPLOY_BUCKET --delete && aws cloudfront create-invalidation --distribution-id $CLOUDFRONT_DISTRIBUTION_ID --paths \"/*\"",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "next": "^14.0.0",
    "@apollo/client": "^3.9.0",
    "graphql": "^16.8.0",
    "graphql-ws": "^5.14.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.9.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0"
  },
  "export": "next export" 
}
